# Интернационализация (i18n) Boxer Platform

## Установленные библиотеки

- `i18next` - библиотека для интернационализации
- `react-i18next` - интеграция i18next с React
- `i18next-browser-languagedetector` - автоматическое определение языка браузера

## Поддерживаемые языки

- 🇷🇺 Русский (ru) - язык по умолчанию
- 🇬🇧 English (en)
- 🇰🇬 Кыргызча (ky)

## Структура файлов

```
src/
  i18n/
    config.js          # Конфигурация i18next
    locales/
      ru.json          # Русские переводы
      en.json          # Английские переводы
      ky.json          # Киргизские переводы
  components/
    LanguageSwitcher.jsx  # Компонент переключателя языков
```

## Использование в компонентах

### Импорт хука useTranslation

```javascript
import { useTranslation } from 'react-i18next'

export default function MyComponent() {
  const { t, i18n } = useTranslation()
  
  return (
    <div>
      <h1>{t('nav.home')}</h1>
      <p>{t('home.hero.subtitle')}</p>
    </div>
  )
}
```

### Смена языка программно

```javascript
const { i18n } = useTranslation()

// Сменить язык
i18n.changeLanguage('en')

// Получить текущий язык
const currentLanguage = i18n.language
```

### Переводы с параметрами

```javascript
// В JSON файле перевода
{
  "welcome": "Добро пожаловать, {{name}}!"
}

// В компоненте
t('welcome', { name: user.name })
```

### Переводы со значением по умолчанию

```javascript
t('someKey', { defaultValue: 'Текст по умолчанию' })
```

## Структура ключей перевода

Переводы организованы по разделам:

- `nav` - навигация
- `user` - пользовательские данные
- `home` - главная страница
- `about` - страница о бойце
- `courses` - курсы
- `courseDetail` - детали курса
- `dashboard` - панель управления
- `profile` - профиль
- `cart` - корзина
- `shop` - магазин
- `payments` - оплата
- `auth` - авторизация/регистрация
- `common` - общие элементы (кнопки, сообщения)
- `errors` - сообщения об ошибках

## Добавление новых переводов

1. Откройте файлы `src/i18n/locales/{ru,en,ky}.json`
2. Добавьте новый ключ в нужную секцию во всех трех файлах
3. Используйте этот ключ через хук `t()` в компоненте

Пример:
```json
// ru.json
{
  "nav": {
    "newPage": "Новая страница"
  }
}

// en.json
{
  "nav": {
    "newPage": "New Page"
  }
}

// ky.json
{
  "nav": {
    "newPage": "Жаңы бет"
  }
}
```

## Обновленные компоненты

### ✅ Полностью переведены:
- Navbar
- LanguageSwitcher
- Login
- Register
- Cart (частично)

### 🔄 Требуют обновления:
- Home
- About
- Courses
- CourseDetail
- CoursePlayer
- Dashboard
- Profile
- MyCourses
- Orders
- Merch
- Payments
- Lesson
- NotFound
- AIChat
- CourseCard
- MerchCard

## Рекомендации

1. **Всегда используйте ключи перевода** вместо жестко закодированных строк
2. **Группируйте переводы** по логическим секциям (страница, компонент)
3. **Используйте понятные имена ключей** (например, `home.hero.title` вместо `title1`)
4. **Избегайте дублирования** - используйте `common` для повторяющихся элементов
5. **Тестируйте на всех языках** перед деплоем

## Пример обновления компонента

### До:
```javascript
export default function MyComponent() {
  return (
    <div>
      <h1>Добро пожаловать</h1>
      <button>Начать</button>
    </div>
  )
}
```

### После:
```javascript
import { useTranslation } from 'react-i18next'

export default function MyComponent() {
  const { t } = useTranslation()
  
  return (
    <div>
      <h1>{t('welcome.title')}</h1>
      <button>{t('common.start')}</button>
    </div>
  )
}
```

## Сохранение выбранного языка

Язык автоматически сохраняется в `localStorage` и восстанавливается при следующем посещении благодаря `i18next-browser-languagedetector`.

## Отладка

Для включения режима отладки измените в `src/i18n/config.js`:
```javascript
debug: true
```

Это выведет в консоль информацию о загруженных переводах и отсутствующих ключах.
